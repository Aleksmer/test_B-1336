mixin card-service(img, title)
  .card-service.swiper-slide
    .card-service__img
      img(src=img)
    .card-service__gradient
    .card-service__info
      h4.card-service__title=title
      h6.card-service__description
        block
